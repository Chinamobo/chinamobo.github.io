// Generated by CoffeeScript 1.6.2
(function() {
  var D, W, c, lastScale, maxScale, minScale, root;

  W = window;

  D = document;

  if (NO && navigator.userAgent.match(/(iPhone|iPad)/i)) {
    root = D.getElementById("content");
    maxScale = 200;
    minScale = 25;
    lastScale = NaN;
    c = 0;
    D.addEventListener("gesturestart", (function() {
      lastScale = parseInt(root.style.fontSize);
      if (!(lastScale > 0)) {
        lastScale = 100;
      }
    }), true);
    D.addEventListener("gesturechange", (function(event) {
      var scale, size;

      scale = event.scale;
      if (typeof scale === 'number') {
        size = (scale - 1) / 4 * 100 + lastScale;
        if (size > maxScale || size < minScale) {
          return;
        }
        root.style.fontSize = size + '%';
      }
    }), true);
  }

}).call(this);
